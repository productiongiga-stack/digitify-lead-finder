{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @digitify/db db:generate && pnpm --filter @digitify/web build",
  "outputDirectory": "apps/web/.next",
  "crons": [
    {
      "path": "/api/cron/drip",
      "schedule": "0 8 * * *"
    },
    {
      "path": "/api/cron/bookings-sync",
      "schedule": "30 8 * * *"
    }
  ]
}
